<h1>Delicious Burgers!</h1>

<h2>Burgers that are not devoured!  Are you hungry?</h2>

<ul>
  {{#each burgers}}
    {{#unless devour}}
      {{> burgers/burger-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are devoured!  So sad!</h2>

<ul>
  {{#each burgers}}
    {{#if devour}}
      {{> burgers/burger-block devour=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Type:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devour">Who wants a burger?</label><br>
    <input type="radio" name="devour" value="1" checked> Devoured?<br>
    <input type="radio" name="devour" value="0"> Not devoured
  </div>

  <button type="submit">Add Burger</button>
</form>
